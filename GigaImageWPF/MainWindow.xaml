<Window x:Class="GigaImageWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GigaImageWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ПАРАМЕТРЫ -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <ComboBox x:Name="StyleCombo" Width="150" Margin="5">
                <ComboBoxItem Content="Киберпанк"/>
                <ComboBoxItem Content="Минимализм"/>
                <ComboBoxItem Content="Реализм"/>
                <ComboBoxItem Content="Аниме"/>
            </ComboBox>

            <ComboBox x:Name="ColorCombo" Width="150" Margin="5">
                <ComboBoxItem Content="Тёплые"/>
                <ComboBoxItem Content="Холодные"/>
                <ComboBoxItem Content="Пастельные"/>
                <ComboBoxItem Content="Контрастные"/>
            </ComboBox>

            <ComboBox x:Name="AspectCombo" Width="150" Margin="5">
                <ComboBoxItem Content="16:9"/>
                <ComboBoxItem Content="21:9"/>
                <ComboBoxItem Content="4:3"/>
            </ComboBox>

            <Button Content="Календарь"
                    Width="120"
                    Margin="5"
                    Click="Calendar_Click"/>
        </StackPanel>

        <!-- ПРОМПТ -->
        <TextBox x:Name="PromptBox"
                 Grid.Row="3"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontSize="14"
                 Margin="5"
                 Height="250"
                 Text="Введите описание изображения"/>

        <!-- КНОПКИ -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Сформировать промпт"
                    Width="200"
                    Margin="5"
                    Click="GeneratePrompt_Click"/>
            <Button Content="Сгенерировать "
                    Width="150"
                    Margin="5"
                    Click="Generate_Click"/>
        </StackPanel>
    </Grid>
</Window>
